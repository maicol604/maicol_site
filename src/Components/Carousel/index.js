import React from 'react';
import './styles.scss';

const Carousel = (props) => {
    return (
        <React.Fragment>

        </React.Fragment>
    )
}
Carousel.defaultProps = {
    elements: []
}
export default Carousel;
